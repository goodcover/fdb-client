name: Pull Request

on:
  pull_request:
    types: [opened, reopened, synchronize]
  push:
    branches: [master]

jobs:

  tests:
    runs-on: ubuntu-latest
    permissions:
      checks: write
      contents: read
      pull-requests: write
    strategy:
      matrix:
        # Update scala-version in build.mill as well
        scala-version: ["2.13.18", "3.3.8"]
        spark-version: ["3.5.9", "4.0.1"]
    steps:
      - name: Checkout sources
        uses: actions/checkout@v7
      - name: Setup caching and environment
        uses: coursier/cache-action@v8
        id: coursier-cache
      - name: Setup FoundationDB
        uses: ./actions/setup-fdb
        with:
          fdb_version: 7.3.43
      - name: Setup Nix
        uses: DeterminateSystems/nix-installer-action@main
      - name: Check Nix flake
        uses: DeterminateSystems/flake-checker-action@main
      - name: Prepare Nix environment
        run: nix develop . --command printenv
      - name: Run tests for Scala ${{ matrix.scala-version }}
        run: nix develop . --command ./mill __[${{ matrix.scala-version }}].test
      - name: Run tests for Spark ${{ matrix.spark-version }} Scala ${{ matrix.scala-version }}
        run: nix develop . --command ./mill __[${{ matrix.scala-version }},${{ matrix.spark-version }}].test
      - name: Gather coverage report for Scala ${{ matrix.scala-version }}
        run: nix develop . --command ./mill scoverage.xmlReportAll
      - name: Upload coverage report for Scala ${{ matrix.scala-version }}
        uses: codecov/codecov-action@v7
        with:
          fail_ci_if_error: false
          files: out/scoverage/xmlReportAll.dest/scoverage.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: scala-${{ matrix.scala-version }}

      - name: Publish Test Report for Scala ${{ matrix.scala-version }}
        uses: mikepenz/action-junit-report@v6
        if: always() # always run even if the previous step fails
        with:
          fail_on_failure: false
          include_passed: false
          detailed_summary: true
          annotate_only: true
          require_tests: false
          report_paths: 'out/**/test-report.xml'
      - name: Upload test results for Scala ${{ matrix.scala-version }}
        uses: codecov/codecov-action@v7
        with:
          report_type: test_results
          fail_ci_if_error: false
          name: codecov-scala-${{ matrix.scala-version }}
          token: ${{ secrets.CODECOV_TOKEN }}
